<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MusicPlayer" parent="Base.Theme.MusicPlayer" />

    <style name="MySeekBar">
        <item name="android:progressTint">@color/col1</item>
        <item name="android:progressBackgroundTint">@color/col2</item>
        <item name="android:thumbTint">@color/col3</item>

    </style>
</resources>